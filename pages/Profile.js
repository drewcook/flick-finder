import App from "../client/components/App";

const Profile = (props) => (
	<App title="Profile">
		<div>
			<h2>Profile</h2>
			<p>Here are some details of the user.</p>
		</div>
	</App>
);

export default Profile;
